/**
 * Stellar / Soroban contract bindings and helpers.
 *
 * These are intentionally lightweight stubs so the frontend and indexer can
 * build and submit transactions with `@stellar/stellar-sdk` directly. After
 * deploying the contracts, run `pnpm gen:soroban` to replace this directory
 * with full TypeScript bindings generated by `stellar-cli`.
 */
import { STELLAR_NETWORK, STELLAR_NETWORK_PASSPHRASE, STELLAR_RPC_URL, STELLAR_HORIZON_URL, STELLAR_EXPLORER_URL, USDC_CONTRACT_ID, USDC_DECIMALS } from "../constants.js";
export { STELLAR_NETWORK, STELLAR_NETWORK_PASSPHRASE, STELLAR_RPC_URL, STELLAR_HORIZON_URL, STELLAR_EXPLORER_URL, USDC_CONTRACT_ID, USDC_DECIMALS, };
/** Pull these from your environment after deploying to Stellar testnet. */
export declare const FACTORY_CONTRACT_ID: string;
export declare const projectFactoryMethods: readonly ["init", "create_project", "project_count"];
export declare const projectEscrowMethods: readonly ["init", "contribute", "submit_milestone", "vote", "resolve_milestone", "claim_milestone", "voting_power_of", "milestone_allocated", "milestone_status"];
export declare const positionNftMethods: readonly ["init", "init_escrow", "mint", "position_of", "token_uri", "owner_of", "balance_of", "name", "symbol"];
/** Human-readable network label used in UI copy. */
export declare const STELLAR_NETWORK_LABEL = "Stellar Testnet";
//# sourceMappingURL=index.d.ts.map