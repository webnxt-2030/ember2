// Prisma schema for Ember
// Full schema will be defined in a later issue

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
